Here’s a concise summary for an AI agent coding a React Native + TypeScript app with grab and swipe navigation between two pages:

⸻

🧠 Objective:

Implement a grab and swipe gesture to switch between:
	•	/dashboard (main screen)
	•	/chat (chat screen)

Behavior:
	•	Swipe Right → Navigate to /chat
	•	Swipe Left → Navigate back to /dashboard

⸻

🔧 Tools & Libraries:
	1.	React Native Gesture Handler
npm install react-native-gesture-handler
	2.	React Native Reanimated
npm install react-native-reanimated
	3.	React Navigation (Stack or Native Stack)
npm install @react-navigation/native @react-navigation/native-stack

⸻

🛠️ Core Steps:

1. Set Up Navigation

// App.tsx
import { NavigationContainer } from '@react-navigation/native';
import { createNativeStackNavigator } from '@react-navigation/native-stack';

const Stack = createNativeStackNavigator();

<NavigationContainer>
  <Stack.Navigator screenOptions={{ headerShown: false }}>
    <Stack.Screen name="Dashboard" component={DashboardScreen} />
    <Stack.Screen name="Chat" component={ChatScreen} />
  </Stack.Navigator>
</NavigationContainer>


⸻

2. Wrap Screens with Pan Gesture Handler

// SwipeWrapper.tsx
import { PanGestureHandler } from 'react-native-gesture-handler';
import { useNavigation } from '@react-navigation/native';
import Animated, { useAnimatedGestureHandler } from 'react-native-reanimated';

const SwipeWrapper = ({ children, current }: { children: React.ReactNode; current: 'Dashboard' | 'Chat' }) => {
  const navigation = useNavigation();

  const gestureHandler = useAnimatedGestureHandler({
    onEnd: (event) => {
      const { translationX } = event;
      if (translationX > 50 && current === 'Dashboard') {
        // Swipe Right → Go to Chat
        runOnJS(navigation.navigate)('Chat');
      } else if (translationX < -50 && current === 'Chat') {
        // Swipe Left → Back to Dashboard
        runOnJS(navigation.navigate)('Dashboard');
      }
    },
  });

  return (
    <PanGestureHandler onGestureEvent={gestureHandler}>
      <Animated.View style={{ flex: 1 }}>
        {children}
      </Animated.View>
    </PanGestureHandler>
  );
};

export default SwipeWrapper;


⸻

3. Use Wrapper in Screens

// DashboardScreen.tsx
export default function DashboardScreen() {
  return (
    <SwipeWrapper current="Dashboard">
      {/* Your dashboard content */}
    </SwipeWrapper>
  );
}

// ChatScreen.tsx
export default function ChatScreen() {
  return (
    <SwipeWrapper current="Chat">
      {/* Your chat content */}
    </SwipeWrapper>
  );
}


⸻

✅ Notes:
	•	Gesture sensitivity (translationX > 50) can be adjusted.
	•	Wrap the App root with GestureHandlerRootView if needed.
	•	Use runOnJS() to call navigation safely inside gesture handler.

⸻

Let me know if you want animation added to the swipe transition or velocity-based logic.